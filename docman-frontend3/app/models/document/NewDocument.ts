export class NewDocument {
  documentTitle: string
  documentData: string

  constructor(documentTitle: string, documentData: string) {
    this.documentTitle = documentTitle
    this.documentData = documentData
  }
}
